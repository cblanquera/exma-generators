{
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build:frui",
    "build:frui": "cd packages/exma-frui && npm run build"
  },
  "devDependencies": {
    "concurrently": "7.6.0"
  }
}